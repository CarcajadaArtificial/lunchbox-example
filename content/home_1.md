- The `<Main/>`, `<Header/>`, `<Footer/>`, `<Layout/>`, and `<Navigation/>` as the "page layout components" and that they will be tested with more rigor in another page.
- The `<Panel/>` component as the base of many other components.